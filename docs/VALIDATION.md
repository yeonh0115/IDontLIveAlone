# 검증 결과 · 2026-09-17

| 검사 | 결과 |
| --- | --- |
| 서버 `clean build` | UTC·Asia/Seoul 각각 48개 통과, 실패·오류·skip 0, 실행 JAR 생성 |
| Android `assembleDebug testDebugUnitTest lintDebug` | 빌드 성공, 14개 통과, lint 오류 0·경고 194 |
| Pi A 모의 테스트 | 33개 통과 |
| Pi B 모의 테스트 | 13개 통과 |
| 공통 장치 연결 helper | 6개 통과 |
| SD bootstrap | 6개 통과 |
| PC 리포트 | 27개 모의 테스트 및 문법 검사 통과 |

서버는 Java21.0.6·Gradle9.4.1·H2를 사용했습니다. 얼굴 작업 재전송·임대·중복 처리, 동시 연결 코드 claim, 역할 중복, 계정 위조, 영상 소유자 분리, 사진 OFF, 임시 얼굴 파일 정리를 검증했습니다. H2 통과가 운영 MySQL 마이그레이션이나 현장 장치 동작을 보장하지는 않습니다.

Pi 테스트는 실제 하드웨어 대신 카메라·GPIO·네트워크 대체 객체를 사용했습니다. 클라우드 페어링 대기와 로컬 카메라·얼굴 인식의 시작을 분리했습니다. 실제 SD에서 GPIO24·인식 기준80·연속2회와 기존 모델 경로를 읽기 전용으로 확인했고 설치 인수에 보존합니다.

Android 검사는 2026-09-17 최종 소스 기준입니다. 서버의 오프셋 없는 KST 시간을 UTC로 오해해 9시간을 더하던 표시를 수정하고 명시 오프셋·잘못된 값도 검사했습니다. 기존 Glide NotificationTarget의 사용하지 않는 알림 권한 진단만 좁게 예외 처리했습니다. 원래 Android 작업 폴더로 변경 소스를 동기화하고 바이트 일치를 확인했습니다. 사용자는 현재 휴대폰 시험이 불가능하다고 답했습니다. 실제 APK 설치·로그인·장치 연결은 아직 미검증입니다.

PC 리포트 테스트는 유료 OpenAI 요청을 실행하지 않습니다. 로그 조회 실패의 디스크 대기열, 재시작 재시도, 중복 생성 비용 방지, REPORT 연결 코드 계약을 모의 검증했습니다.

## 배포 전 백업·연결 확인

- GitHub의 원본·최초 수정·네트워크 원본 annotated checkpoint tags를 확인했습니다.
- Pi SD 전체 15,931,539,456바이트를 비공개 PC 이미지로 백업했습니다. SHA256은 `c6c29cb93632cbd02930e519c526547f2da1322c51e5ad889d79c34ac671d8d1`입니다.
- 기존 Render 사진478개(4,382,425바이트)의 PC 백업을 완료하고 서버 원본 파일 해시와 일치시켰습니다. 이후 사용자 요청으로 이벤트 사진 보관을 끄며, 유료 저장소를 추가하거나 기존 사진을 새 서버로 이관하지 않습니다.
- 학교망 PC에서 Render HTTPS/TLS443은 성공하고 Aiven14640 직접 연결은 시간 초과였습니다. 서버의 DB를 사용하는 읽기 요청은 정상 응답했습니다. 학교망 포트 제한과 일치하는 현상이며 원인 장비까지 확정한 것은 아닙니다.
- 운영 Aiven DB를 기존 Render 연결의 READ ONLY consistent snapshot으로 백업했습니다. 14개 InnoDB 테이블·1,324행이며 PC gzip 무결성·행 수·서버 해시 일치를 검증했습니다. SQL 백업 SHA256: `3d3091d7277178fc730f7678c4191898bb8f91c13072516fe1b7cef54ab4bc46`. 별도 DB로의 복원 리허설은 하지 않았습니다.

## 실제 적용 진행 기록

2026-09-17 운영 DB 백업 후 첫 Render 배포(`3782e4b`)는 테스트 5개 실패로 중단됐고 기존 운영 서버는 유지됐습니다. UTC 환경에서 같은 실패를 재현했습니다. DB 초기화 뒤 `@PostConstruct`에서 JVM 시간대를 변경해 JDBC의 날짜 변환 기준이 달라지는 것이 원인이었습니다. 시간대 설정을 `main`의 Spring 초기화 앞으로 이동하고, 반복 저장 때 실제 SQL DATE와 JPA 날짜가 유지되는 회귀 테스트를 추가했습니다. 테스트를 생략하지 않고 수정본을 재검증한 뒤 재배포합니다.

수정본 `6195acd815a54517c1a5573384efdaf71adac989`의 Render Linux `clean build`와 배포가 성공했습니다. 2026-09-17 16:07 KST에 서비스가 Live 상태가 되었으며 배포 ID는 `dep-dalp1uf40ujc73eq0vh0`입니다. 실제 서버에서 DB를 확인하는 `/healthz` 200, 인증 없는 장치 상태·목록·카메라 API 401, 사진 업로드 410(disabled)을 확인했습니다.

준비한 실제 Pi A CAMERA 인증정보로 등록/재등록/상태 조회를 확인했습니다. 같은 인증정보가 같은 장치와 연결 코드를 유지하며, 앱에 연결되지 않은 CAMERA의 작업 접근은 403으로 거부됩니다. 임의 테스트 계정·센서 로그·리포트는 운영 DB에 만들지 않았습니다.

Pi A SD의 장치 번호·USB 일련번호·크기·파티션과 원본 cmdline 해시를 확인하고 설치 파일 11개를 복사했습니다. 전체 파일 검증 후 마지막에 첫 부팅 설치 옵션을 적용했고 쓰기 캐시를 비웠습니다. 원래 OS·Wi-Fi·얼굴 모델을 유지하는 설치이며 포맷하지 않았습니다. 사용자가 SD를 Pi에 넣고 전원을 켜기로 했으나 설치 완료 로그·실제 카메라·문 잠금 결선·얼굴 인식률·휴대폰 동작은 아직 확인하지 못했습니다.

운영 DB 백업에 사용한 임시 Render SSH 키는 작업 후 삭제했으며 계정에 `No authorized SSH keys`가 표시됨을 확인했습니다.

## 산출물 해시

- 서버 JAR(56,960,792바이트): `82ccc07c6173c4a90ef3b155c954ade2f25bea2b56ab0df5d5ac1da1ca5f9e7d`
- Android debug APK: `906880a91c6dd678b032263add6977f4d135496e587a4115bbd503dfd581b9d1`

Docker 실행기가 없어 로컬 컨테이너 빌드는 하지 않았고, Render의 실제 Linux 컨테이너 빌드 성공으로 확인했습니다. 상세 적용 순서는 [DEPLOYMENT.md](DEPLOYMENT.md)에 있습니다.
