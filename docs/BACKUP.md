# Source backup before reliability fixes

Date: 2026-09-16 (Asia/Seoul).

The repository root preserves the existing GitHub Spring Boot server. `android/` contains the Android source and Gradle wrapper from the supplied Android project. `raspberrypi/` contains the exact supplied `fin_camera.py` and `fin_face.py` files for Raspberry Pi A. File hashes are recorded in `backup-manifest-2026-09-16.json`.

Build outputs, SDK paths, IDE state, documents, videos, face training data, and unrelated Raspberry Pi B programs are excluded. No running hardware or database was changed.
